library(devtools)
library(data.table)
library(ggplot2)
library(base64enc)
library(yaml)

# ------------------------
# Local environments
# ------------------------
data_env <- new.env()
vis_env <- new.env()